---
import { cn } from "@lib/utils";

type Props = {
  className?: string;
  id: string;
};

const { id, className } = Astro.props;
---

<div id={id} class={cn("absolute inset-0 opacity-0 pointer-events-none overflow-hidden", className)}>
  <slot />
</div>
