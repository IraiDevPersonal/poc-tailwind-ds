---
import image from "@assets/slider-img.png";
import Slide from "./slider/Slide.astro";

type Props = {
  name: string;
};

const { name } = Astro.props;
---

<Slide>
  <img class="w-full object-contain h-40" src={image.src} alt={name} />
</Slide>
