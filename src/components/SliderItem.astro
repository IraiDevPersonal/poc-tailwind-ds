---
import { cn } from "../lib/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<div
  class={cn(
    "embla__slide flex-none w-50 h-50 bg-white rounded-2xl shadow p-4 flex flex-col",
    className,
  )}
>
  <slot />
</div>
