---
import { cn } from "../../lib/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<div class={cn("flex flex-col gap-4 w-full min-w-0", className)}>
  <slot />
</div>
