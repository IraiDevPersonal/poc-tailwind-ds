---
import { cn } from "@lib/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<div class={cn("relative flex gap-3 w-full min-w-0", className)}>
  <slot />
</div>
