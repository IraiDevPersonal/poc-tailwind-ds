---
import { cn } from "@lib/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<li>
  <button
    class={cn(
      "h-12 w-12 min-w-12 bg-white rounded-lg p-2 text-xs ring-2 ring-transparent transition-all ease-in-out data-[active=true]:ring-red-500 truncate",
      className,
    )}
  >
    <slot />
  </button>
</li>
